NODE_ENV=development
PORT=3001

# JWT (use a long random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Database (PostgreSQL)
DATABASE_URL=postgresql://updoo:updoo@localhost:5432/updoo

# Google API key (e.g. Gemini AI; Gmail sending uses OAuth refresh token, not this key)
GOOGLE_API_KEY=your-google-api-key

# Google OAuth (login with Google) – copy to .env and paste real values from Google Cloud Console
GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3001/auth/google/callback
FRONTEND_URL=http://localhost:3000

# MailerSend (sending emails)
# Get API key from https://www.mailersend.com/ – verified domain required for production
MAILERSEND_API_KEY=your-mailersend-api-key
MAILERSEND_FROM_EMAIL=noreply@oferi.eu
MAILERSEND_FROM_NAME=Oferi
# Webhook signing secret (from MailerSend webhook settings) – required for POST /webhooks/mailersend
MAILERSEND_WEBHOOK_SECRET=your-webhook-signing-secret
# Optional: default recipient for send-test script
# TEST_EMAIL_TO=your@email.com

# Content generator cron: max AI-generated job posts per day (6:00–23:59, random times)
CONTENT_GENERATOR_MAX_POSTS_PER_DAY=5

# Dev only: Nest debug logs (e.g. nest:*)
# DEBUG=nest:*
